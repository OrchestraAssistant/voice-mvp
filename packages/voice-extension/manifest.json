{
  "manifest_version": 3,
  "name": "Voice (persistent)",
  "version": "0.2.0",
  "description": "A voice session that lives in the browser, not the page: it survives navigations and full reloads, and binds to a page's manifest when it serves one.",

  "//": "The session lives in the offscreen document (survives page loads); the content script is the per-page DOM/API bridge; the popup grants the mic once for the extension origin. tabs/scripting are the browser-orchestration core.",
  "permissions": ["tabs", "scripting", "offscreen", "storage"],
  "host_permissions": ["<all_urls>"],

  "background": { "service_worker": "dist/background.js", "type": "module" },

  "action": { "default_popup": "popup.html", "default_title": "Voice: start a session on this tab" },

  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["dist/content.js"],
      "run_at": "document_idle"
    }
  ]
}
